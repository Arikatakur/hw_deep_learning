# Dataset Folder

Place original collected JPG images in `raw/`.

After running the split script, images will be copied into:

- `train/images/`
- `test/images/`

Only the training images need labels for Phase 2.
